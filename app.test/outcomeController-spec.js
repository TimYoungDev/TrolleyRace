var dbMock = require('./mocks/trolleyRaceDbMock');
var controller = require('../app.api/outcome/outcomeController')(dbMock);

describe("Outcome Controller", function () {
    describe("getEmailList", function () {
        
    });
});
